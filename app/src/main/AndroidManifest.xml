<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.movie.app">

    <application
        android:name="com.movie.app.MainApp"
        android:allowBackup="true"
        android:icon="@drawable/movie_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/movie_splash"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.movie.app.splashscreen.SplashActivity" android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>